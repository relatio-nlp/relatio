name: narrative-nlp-dev
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.7
  - jupyter
  - nbconvert
  - pandas
  - nltk
  - gensim
  - scikit-learn
  - pytest
  - black
  - pytorch # pytorch is needed by allennlp and from pip not working on macOS
  - pip
  - pip:
      - allennlp
      - tensorflow-hub
