[tox]
envlist = py37,py38,py39,dev

[testenv]
deps = -r.binder/requirements_test.txt
commands = 
    pip install -e .
    pytest --cov=narrativeNLP
    black --check .

[testenv:dev]
basepython = python3.7
usedevelop = True
deps = -r.binder/requirements_dev.txt
commands = 
    pip install -e .
